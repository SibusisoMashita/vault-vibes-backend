spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/vaultvibes
    username: postgres
    password: postgres
  flyway:
    default-schema: public
    schemas: public

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
      - https://vaultvibes.co.za

