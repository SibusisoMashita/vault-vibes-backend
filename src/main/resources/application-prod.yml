spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DB_URL:jdbc:postgresql://prod-omnisolve-postgres.cmrg4wcome3h.us-east-1.rds.amazonaws.com:5432/vaultvibes}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  flyway:
    default-schema: public
    schemas: public

app:
  cors:
    allowed-origins: https://vaultvibes.co.za

